const { zip } = require('..')
const fs = require('fs')

// describe('lib/index.js', () => {
//   // jest.useFakeTimers()
//   // Date.now = jest.fn(() => 1503187200000)
//   test('zip self node_modules', async () => {
//     await zip('node_modules')
//     expect(fs.existsSync('node_modules.zip')).toBe(true)
//   })
// })
